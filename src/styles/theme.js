import { createTheme } from '@mui/material/';
const theme = createTheme({
  palette: {
    primary: {
      main: '#81B214',
      white: '#FFF'
    },
    secondary: {
      main: '#777777',
      light: '#D3D3D3'
  },
  }
})

export default theme;