import { createTheme } from '@mui/material/';
const theme = createTheme({
  palette: {
    primary: {
      main: '#81B214',
      white: '#FFF',
      black: '#000',
    },
    secondary: {
      main: '#777777',
      light: '#D3D3D3',
      verylight: '#fafafa',
  },
  }
})

export default theme;